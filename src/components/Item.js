// Item.js

import React, {useState} from 'react';
import ReactDOM from 'react-dom/client';

function Item(props) {
  const notSelected = () => { 

  }
  return (
    <div id = {this.props.key}>
      {this.props.value}
    </div>
  );
}

export default Item;
